# Vercel / local: copy to .env.local for `vercel dev` (never commit real keys).
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# VAT protocol gate (/protocol/* rewrites). Use long random strings.
PROTOCOL_PASSPHRASE=
PROTOCOL_COOKIE_SECRET=
